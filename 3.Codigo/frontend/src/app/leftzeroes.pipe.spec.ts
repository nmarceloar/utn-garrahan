import { LeftzeroesPipe } from './leftzeroes.pipe';

describe('LeftzeroesPipe', () => {
  it('create an instance', () => {
    const pipe = new LeftzeroesPipe();
    expect(pipe).toBeTruthy();
  });
});
