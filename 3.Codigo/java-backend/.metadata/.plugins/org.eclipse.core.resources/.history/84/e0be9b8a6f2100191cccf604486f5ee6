package p2018.backend.repository;

import org.springframework.data.jpa.repository.JpaRepository;
import org.springframework.data.jpa.repository.Query;
import org.springframework.data.repository.query.Param;

import p2018.backend.entities.OrderInfo;

public interface OrderRepository extends JpaRepository<OrderInfo, Long> {
	
	@Query("select count(*) from OrderInfo o where o.institutionId = :institutionId")
	Integer findOrderCountByInstitytionId(@Param("institutionId") Long institutionId);
}
